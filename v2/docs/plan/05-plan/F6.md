# Fase F6 — Cliente CLI/batch e integración final (Seguimiento)

> Documento de seguimiento generado a partir de
> [`05-plan-ejecucion.md`](../05-plan-ejecucion.md).

## 1. Ficha de la fase

| Campo | Valor |
|---|---|
| **Código** | F6 |
| **Nombre** | Cliente (CLI/batch) e integración final |
| **Objetivo / alcance** | Entregar el cliente `voucherflow` (CLI con subcomandos + modo batch con workers/checkpoints/enfriamiento), sidecars con trazabilidad, mapa de paridad v1→v2 sobre carpetas reales de `files/` y documentación de usuario. |
| **Épicas asociadas** | E-CLI (E-CLI, E-CLI-1/3, E-CLI-2) |
| **Depende de (fases)** | F1-F5 (capacidades expuestas por subcomando) |
| **Habilita a (fases)** | — (fase final; corte de v1 al confirmar paridad) |
| **Prioridad MoSCoW** | Must (MVP) para E-CLI-1/2; matiz: E-CLI-3 (enfriamiento/retries avanzados) → Should y T-606 API HTTP → fase 2 / no bloqueante para el MVP (ver MoSCoW §4 del doc 05) |
| **Estimación (dh)** | 6 (optimista 4 / pesimista 9) |
| **Responsable ciclo** | team analysis (BA/SA/PM) → team implementation |
| **Estado fase** | 🟡 En implementación (T-601 hecha; T-602..T-606 pendientes) |
| **DoR cumplido** | [x] sí |
| **Fecha inicio** | 2026-09-12 |
| **Fecha fin** |  |

## 2. Definition of Done de la fase (DoD)

> _Copia literal del bloque "DoD de F6" del documento fuente._

- [ ] Los comandos de v1 tienen equivalente en v2 con resultados comparables (o mejores) sobre una muestra acordada de `files/`; documentación lista.

## 3. Tareas y seguimiento (WBS)

### T-601 · CLI `voucherflow` con subcomandos (process/validate/classify/extract/run/batch/ask/arca/case/hitl)
- **Depende de**: F1-F5
- **Épica**: E-CLI
- **Estado**: [ ] Pendiente · [ ] En desarrollo · [ ] En QA · [x] Hecho
- **Entregable / definición de terminado**: El CLI `voucherflow` expone los subcomandos process/validate/classify/extract/run/batch/ask/arca/case/hitl para archivo o carpeta recursiva, respetando `--force`, `--orientation`, `--condicion-impositiva`, `--model` y `--workers`.
- **Implementación**: el cliente se apoya en dos piezas que estaban como esqueleto de F0 y T-601 llena. El **orquestador** (`orchestrator.py`) encadena processing → validation → extraction → combinación → conclusión → traza y devuelve el `PipelineResult` con el `VoucherResult`, la `CombinedEvidence` y el `CaseRecord`; incluye `ejecutar_lote` (secuencial y determinista en T-601), `iterar_documentos` (carpeta recursiva que **excluye** los artefactos derivados: `*.case.json`, `*_pipeline.json`, `*_classification.json`, `*.raw.md`) e `identificador_de_archivo` (sha256 en bloques). El **fast-fail del gate** (F2) resuelve un no-comprobante como `rechazado` con certeza `alta` y origen `programa` **sin** gastar extracción. La **fachada** (`api.py`) implementa `extract` (procesa + gate + flujos + combinación → `CombinedEvidence`), `run` (→ `VoucherResult`) y `ask` (pregunta puntual, equiv. `v1/ask.py`), todos con colaboraciones inyectables. El **cliente** (`cli/main.py`) usa `argparse` de la stdlib (sin dependencias nuevas; el extra `cli` queda vacío y el entry point es `[project.scripts] voucherflow`), escribe el dato a `stdout` y el progreso/error a `stderr`, y `main()` **devuelve** el código de salida. Los once subcomandos delegan en `api.*`/`orchestrator.*`: la CLI no reimplementa reglas (E-LIB-1).
- **Verificación**: suite completa **1460 passed / 10 skipped** (57 nuevos de `tests/test_cli_t601.py` sobre los 1403 de T-507, con `test_golden_y_esqueleto.py` reescrito: la lista de esqueletos de la fachada queda **vacía**); `python scripts/F6/t601.py` → **9/9** escenarios + **6/6** fronteras — exit 0.
- **Hallazgo 1 (las fronteras se reescriben, no se borran)**: `test_esqueletos_lanzan_notimplemented` era la lista de pendientes de la fachada; con T-601 queda vacía. Se reescribió para fijar la frontera nueva —ninguna operación pública lanza `NotImplementedError` y el orquestador resuelve el archivo inexistente con el resultado declarado— en vez de eliminar la aserción (misma lección que F5/T-505).
- **Hallazgo 2 (`procesar_imagen` tiene su propio gate, distinto del qween)**: ejercitar el fast-fail de F2 con una imagen exigiría que el binario tuviera dimensiones legibles; los tests usan **texto nativo** para no acoplar la orquestación al fixture binario. Son dos controles distintos y corren en momentos distintos del pipeline.
- **Hallazgo 3 (no confundir «no se pudo leer» con «rechazado»)**: el orquestador devuelve `ok=False` para el archivo ilegible y `ok=True` + `estado=rechazado` para el gate negativo; la fachada convierte **solo** el primero en `DocumentoNoProcesableError`. Aplanarlos habría hecho que el cliente tratara un rechazo firme como una excepción de infraestructura.
- **Nota (alcance honesto)**: el modo heredado de v1 (`--mode kvi|kvg|10|11`) se registra en la traza para la paridad de T-604, pero v2 tiene **un** contrato de extracción versionado: fingir una diferencia que no existe haría la paridad inauditable. El lote de T-601 es secuencial y lo declara (`max_workers_solicitado` vs. `aplicado`); el pool, los checkpoints y el enfriamiento son T-602.
- **Responsable**: team analysis / team implementation

### T-602 · Modo batch con workers, checkpoints y política de enfriamiento (ADR-010)
- **Depende de**: T-601
- **Épica**: E-CLI-1/3
- **Estado**: [ ] Pendiente · [ ] En desarrollo · [ ] En QA · [ ] Hecho
- **Entregable / definición de terminado**: El modo batch corre con workers (cada uno con su convertidor Docling), checkpoints/reanudación y la política de enfriamiento del ADR-010 (la cuenta inicia cuando TODOS los workers están detenidos). E-CLI-3 (retries/backoff avanzados) → Should.
- **Responsable**: team analysis / team implementation

### T-603 · Sidecars con trazabilidad y salida agregada
- **Depende de**: T-601
- **Épica**: E-CLI-2
- **Estado**: [ ] Pendiente · [ ] En desarrollo · [ ] En QA · [ ] Hecho
- **Entregable / definición de terminado**: Cada resultado genera un JSON sidecar (resultado + evidencia + trazabilidad) y en modo lote se puede consolidar en un único JSON agregado.
- **Responsable**: team analysis / team implementation

### T-604 · Mapa de paridad v1→v2 verificado sobre carpetas reales de `files/`
- **Depende de**: F1-F5
- **Épica**: E-CLI
- **Estado**: [ ] Pendiente · [ ] En desarrollo · [ ] En QA · [ ] Hecho
- **Entregable / definición de terminado**: Los comandos de v1 tienen equivalente en v2 con resultados comparables (o mejores) sobre una muestra acordada de `files/` (DoD de F6); aquí se declara el corte de v1.
- **Responsable**: team analysis / team implementation

### T-605 · Documentación de usuario + README v2
- **Depende de**: T-601
- **Épica**: —
- **Estado**: [ ] Pendiente · [ ] En desarrollo · [ ] En QA · [ ] Hecho
- **Entregable / definición de terminado**: Documentación de usuario y README v2 actualizados y listos para el operador.
- **Responsable**: team analysis / team implementation

### T-606 · API HTTP (fase 2, no bloqueante para MVP)
- **Depende de**: T-601
- **Épica**: —
- **Estado**: [ ] Pendiente · [ ] En desarrollo · [ ] En QA · [ ] Hecho
- **Entregable / definición de terminado**: API HTTP básica definida como fase 2 / no bloqueante para el MVP (Should/Could según MoSCoW §4 del doc 05); backlog post-MVP si no entra en el MVP.
- **Responsable**: team analysis / team implementation

## 4. Bitácora de seguimiento de la fase

| Fecha | Acción / hito | Responsable | Estado |
|---|---|---|---|
| 2026-09-12 | **T-601 hecha: CLI `voucherflow` + orquestador + fachada (E-CLI-1/E-CLI-2)**. Los **once** subcomandos (`process`, `validate`, `classify`, `extract`, `extract-detect`, `run`, `batch`, `ask`, `arca`, `case` `show/list`, `hitl list`) delegan en `api.*`/`orchestrator.*` con `argparse` de la stdlib (sin dependencias nuevas; el extra `cli` queda vacío y el entry point es `[project.scripts]`). `main()` **devuelve** el código de salida y el dato va a `stdout` mientras el progreso/error va a `stderr`. El **orquestador** encadena F1→F5 (processing → validation → extraction → combinación → conclusión → traza) y puebla el `PipelineResult` con el `VoucherResult`, la `CombinedEvidence` y el `CaseRecord`; el **fast-fail del gate** resuelve un no-comprobante como `rechazado` con certeza `alta` **sin** gastar extracción; `iterar_documentos` recorre la carpeta recursiva y excluye los artefactos derivados (`*.case.json`, `*_pipeline.json`, `*_classification.json`, `*.raw.md`), que la carpeta `files/` real tiene a montones. La **fachada** implementa `extract`/`run`/`ask` (los dos primeros eran esqueletos de F0). Los flags comunes (`--force`, `--orientation`, `--condicion-impositiva`, `--model`, `--workers`) se aceptan y **su efecto real se declara** (el lote es secuencial hasta T-602). Suites: `tests/test_cli_t601.py` (57) y `scripts/F6/t601.py` (9/9 escenarios + 6/6 fronteras). **Hallazgos**: la aserción de esqueletos de la fachada queda **vacía** y se reescribe para fijar la frontera nueva (no borrarla); `procesar_imagen` tiene su propio gate (T-102), distinto del qween, así que los tests usan texto nativo; y «no se pudo leer» (F1) no es lo mismo que «rechazado» (gate) — aplanarlos convertiría un rechazo firme en excepción de infraestructura. `F6.md` pasa de 🔴 Backlog a 🟡 En implementación. | team implementation | Hecho |
