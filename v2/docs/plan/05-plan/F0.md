# Fase F0 — Fundación: schemas, esqueleto, golden set (Seguimiento)

> Documento de seguimiento generado a partir de
> [`05-plan-ejecucion.md`](../05-plan-ejecucion.md).

## 1. Ficha de la fase

| Campo | Valor |
|---|---|
| **Código** | F0 |
| **Nombre** | Fundación (esqueleto, contratos, dataset) |
| **Objetivo / alcance** | Sentar la base de v2: congelar los schemas de evidencia, crear el esqueleto del paquete `src/`, resolver los ADR bloqueantes, curar el golden set inicial y encapsular config + `OllamaClient` + adaptador Docling. |
| **Épicas asociadas** | E-LIB (E-LIB, E-LIB-2, E-LIB-3), E-DOC (adaptador), E-CONC / calidad (golden set) |
| **Depende de (fases)** | — (fase inicial) |
| **Habilita a (fases)** | F1, F2 (config/`OllamaClient`), F3/F4/F5 (contratos y ADR-001/002/005/006/007 condicionan su diseño), F6 |
| **Prioridad MoSCoW** | Must (MVP) |
| **Estimación (dh)** | 6 (optimista 4 / pesimista 10) |
| **Responsable ciclo** | team analysis (BA/SA/PM) → team implementation |
| **Estado fase** | 🟡 En revisión (implementación de F0 completada por team implementation; resta curación de etiquetas de negocio del golden set con contador) |
| **DoR cumplido** | [x] |
| **Fecha inicio** | 2026-09-06 |
| **Fecha fin** | 2026-09-06 (implementación) |

## 2. Definition of Done de la fase (DoD)

> _Nota: `05-plan-ejecucion.md` no define un bloque "DoD de F0" explícito; el
> siguiente DoD se sintetiza de la **Salida de F0** y del **DoR/DoD general** y
> el **DoD transversal de fase (calidad)** del documento fuente._

- [x] El paquete es instalable (`src/` + `pyproject`) con los **schemas de evidencia validados y congelados** (contrato con criterio de cambio).
- [x] El `OllamaClient` es robusto (retry/backoff, timeout y mensajes claros) y la configuración está centralizada (settings + env).
- [x] El adaptador Docling está encapsulado y listo para F1.
- [x] Existe un **golden set inicial** curado desde `files/` (índice CSV con casos por tipo de entrada; etiquetas de negocio en `pendiente` hasta curación con contador — ver §3.4 y T-004).
- [x] ADR-001/002/005/006/007 resueltos (estado _aceptado_) antes de codificar F3/F4/F5.
- [ ] Golden set actualizado con etiquetas de negocio (contador), métricas de la fase reportadas (ver `06`) y sin deuda técnica bloqueante conocida para la siguiente (DoD transversal de calidad).

## 3. Tareas y seguimiento (WBS)

### T-001 · Definir y congelar schemas de evidencia (pydantic)
- **Depende de**: —
- **Épica**: E-LIB-2
- **Estado**: [x] Hecho
- **Entregable / definición de terminado**: Schemas pydantic de evidencia definidos, validados y congelados como contrato versionado (base del `SourceEvidence`/`CaseRecord` que consumen F3/F4/F5).
- **Implementación**: `v2/src/voucherflow/schemas/evidence.py` y `result.py` (`SCHEMA_VERSION=1.0.0`, `CRITERIO_CAMBIO`, enums, validadores de contrato). Tests en `v2/tests/test_schemas_*.py`.
- **Responsable**: team analysis / team implementation

### T-002 · Crear esqueleto de paquete `src/` + `pyproject` + CI básico
- **Depende de**: T-001
- **Épica**: E-LIB
- **Estado**: [x] Hecho
- **Entregable / definición de terminado**: Paquete instalable con estructura `src/`, `pyproject` y CI básico en verde, con los módulos de los 5 refactors esbozados (sin acoplamiento a scripts v1).
- **Implementación**: `v2/pyproject.toml`, `.github/workflows/ci.yml`, esqueletos de `processing/validation/classification/extraction/conclusion/rules/trace/models(arca)/api.py/orchestrator.py`. CI instalable y suite en verde.
- **Responsable**: team analysis / team implementation

### T-003 · Resolver ADR-001/002/005/006/007 (workshop)
- **Depende de**: T-001
- **Épica**: —
- **Estado**: [x] Hecho
- **Entregable / definición de terminado**: ADR-001/002/005/006/007 resueltos en workshop y registrados con estado _aceptado_ en `04-decisiones-abiertas-adr.md` (mitiga R-01).
- **Implementación**: `04-decisiones-abiertas-adr.md` actualizado (estado Aceptado + referencia a la implementación en `schemas/` y `rules/`).
- **Responsable**: team analysis / team implementation

### T-004 · Curar **golden set inicial** desde `files/` (≥ N casos etiquetados por tipo)
- **Depende de**: —
- **Épica**: E-CONC / calidad
- **Estado**: [x] Hecho (parcial: etiquetas de negocio pendientes de contador)
- **Entregable / definición de terminado**: Golden set inicial curado desde `files/` con ≥ N casos etiquetados por tipo (base de validación de paridad y métricas de todas las fases; mitiga R-06).
- **Implementación**: `v2/tests/golden/` con `casos.csv` (9 casos reales referenciados por ruta: jpg/pdf/png/jpeg), `splits/` (train/eval sin cruce) y README con criterio. Las etiquetas de negocio (letra/condición/veredicto) quedan `pendiente` hasta disponer de OCR (F1/v1) o criterio de contador (06 §3.4).
- **Responsable**: team analysis / team implementation

### T-005 · Configuración centralizada (settings + env) y `OllamaClient` con retry/diagnóstico
- **Depende de**: T-002
- **Épica**: E-LIB-3
- **Estado**: [x] Hecho
- **Entregable / definición de terminado**: Config centralizada (settings + env) y `OllamaClient` con reintentos/backoff, timeout y mensajes claros, probado con mocks (mitiga R-04).
- **Implementación**: `v2/src/voucherflow/settings/config.py` (defaults → YAML → env `VOUCHERFLOW_*`) y `v2/src/voucherflow/models/ollama.py`. Tests con mock HTTP en `v2/tests/test_models_ollama.py` y `test_settings_config.py`.
- **Responsable**: team analysis / team implementation

### T-006 · Adaptador Docling encapsulado
- **Depende de**: T-002
- **Épica**: E-DOC
- **Estado**: [x] Hecho
- **Entregable / definición de terminado**: Adaptador Docling encapsulado como módulo de la librería, listo para ser consumido por el procesamiento de F1.
- **Implementación**: `v2/src/voucherflow/models/docling.py` (`DoclingConverter` + contrato `ProcessedDocument`). Tests de construcción/contrato en `v2/tests/test_models_docling.py`.
- **Responsable**: team analysis / team implementation

## 4. Bitácora de seguimiento de la fase

| Fecha | Acción / hito | Responsable | Estado |
|---|---|---|---|
| 2026-09-06 | F0 iniciada: T-001 schemas congelados + T-002 esqueleto `src/`+`pyproject`+CI + T-003 ADR-001/002/005/006/007 aceptados + T-005 `OllamaClient`/settings + T-006 `DoclingConverter` | team implementation | Hecho |
| 2026-09-06 | Golden set inicial creado (`tests/golden/`, 9 casos reales indexados por ruta, splits train/eval sin cruce). Etiquetas de negocio pendientes de contador (06 §3.4) | team implementation | Parcial |
| 2026-09-06 | Suite de F0 en verde: `python -m pytest tests -q` (70 tests) en env `py313_env` con `pip install -e v2/` | team implementation | Hecho |
| 2026-09-06 | Pendiente para cerrar F0 con negocio: etiquetar letra/condición fiscal/veredicto del golden set (contador) y reportar métricas (06) | team analysis + negocio | Pendiente |
